Run samtools flagstat on a set of CRAM files. 

To run:
```
nextflow run https://github.com/grbot/flagstat --input "/path/*.cram"
```

or

```
nextflow run https://github.com/grbot/flagstat --input "1.cram 2.cram 3.cram"
```
